'use client';
import { useAppData, dataStore } from '@/lib/store';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { Badge } from '@/components/ui/badge';
import { Check, X, Receipt } from 'lucide-react';
export default function TrainerSettlements(){const d=useAppData();const rows=d.trainerWalletEntries||[];const update=(id:string,status:'approved'|'paid'|'rejected')=>dataStore.saveData({trainerWalletEntries:rows.map(x=>x.id===id?{...x,status,reference:status==='paid'?`SET-${Date.now()}`:x.reference}:x)});return <div dir="rtl" className="container mx-auto space-y-6 p-6"><h1 className="text-2xl font-bold"><Receipt className="ml-2 inline"/>تسویه مربیان</h1><Card><CardHeader><CardTitle>درخواست‌های برداشت</CardTitle></CardHeader><CardContent className="space-y-2">{rows.filter(x=>x.type==='withdrawal').map(x=><div key={x.id} className="flex flex-wrap items-center justify-between gap-2 rounded border p-3"><div><b>{x.amount.toLocaleString()} تومان</b><p className="text-sm text-muted-foreground">{x.description} — {new Date(x.at).toLocaleString('fa-IR')}</p></div><div className="flex items-center gap-2"><Badge>{x.status||'pending'}</Badge>{x.status!=='paid'&&x.status!=='rejected'&&<><Button size="sm" variant="outline" onClick={()=>update(x.id,'rejected')}><X className="ml-1 h-4 w-4"/>رد</Button><Button size="sm" onClick={()=>update(x.id,x.status==='approved'?'paid':'approved')}><Check className="ml-1 h-4 w-4"/>{x.status==='approved'?'ثبت پرداخت':'تأیید'}</Button></>}</div></div>)}{!rows.some(x=>x.type==='withdrawal')&&<p className="text-muted-foreground">درخواستی وجود ندارد.</p>}</CardContent></Card></div>}
